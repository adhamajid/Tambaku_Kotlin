<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tambaku10">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TambaKu10">
        <activity android:name=".maag"></activity>
        <activity android:name=".sembelit" />
        <activity android:name=".diare" />
        <activity android:name=".demam" />
        <activity android:name=".flu" />
        <activity android:name=".batuk" />
        <activity android:name=".AboutUs" />
        <activity android:name=".Macam" />
        <activity android:name=".Informasi" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>